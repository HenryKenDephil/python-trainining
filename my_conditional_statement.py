#if, else satement
 #use identation only when using a closed statement. make use of full colon (:) and imsert a space
 #use elif when using multiple conditions
x=10
y=8
if x>y:
    x=x+2
    print(x)
    y=y+3
else:
    x=x-2
    print(x)
    y=y-3
    print(y)
